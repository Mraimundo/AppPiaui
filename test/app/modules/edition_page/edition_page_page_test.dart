import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EditionPagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EditionPagePage(title: 'EditionPage')));
    //  final titleFinder = find.text('EditionPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
