import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MagazineArticlesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MagazineArticlesPage(title: 'MagazineArticles')));
    //  final titleFinder = find.text('MagazineArticles');
    //  expect(titleFinder, findsOneWidget);
  });
}
