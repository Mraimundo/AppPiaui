import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SubscribeNowPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SubscribeNowPage(title: 'SubscribeNow')));
    //  final titleFinder = find.text('SubscribeNow');
    //  expect(titleFinder, findsOneWidget);
  });
}
