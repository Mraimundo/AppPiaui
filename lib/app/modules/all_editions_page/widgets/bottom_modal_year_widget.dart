import 'package:flutter/material.dart';

class BottomModalYearWidget extends StatelessWidget {
  const BottomModalYearWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
